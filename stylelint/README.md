# stylelint-config-nk

## Installation

```sh
npm i stylelint-config-nk -D
```

## Usage

Add the config to your `.stylelintrc`:

```json
{
  "extends": "stylelint-config-nk"
}
```

or `.stylelintrc.js`:

```js
module.exports = {
  extends: 'stylelint-config-nk',
}
```
