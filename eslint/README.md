# eslint-config-nk

## Installation

```sh
npm i eslint-config-nk -D
```

## Usage

Add the config to your `.eslintrc`:

```json
{
  "extends": "eslint-config-nk"
}
```

or `.eslintrc.js`:

```js
module.exports = {
  extends: 'eslint-config-nk',
}
```
